.zig-cache/
zig-out/
# Local / agent scratch (not part of the kernel tree)
.log/
.contents/
# Root output dir only; plain `build/` wrongly ignores `scripts/build/*.sh`.
/build/
mdcs/
external/
run-iso.sh
# Local helper; intentionally not versioned (see script header).
push.sh
firmware/
gnu-efi/
*.o

# Python
__pycache__/
*.py[cod]
*$py.class
