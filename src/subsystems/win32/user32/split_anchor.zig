// SPDX-License-Identifier: MIT OR Apache-2.0
//! Anchor for incremental splits from `user32.zig` (message pump, NC tables, compositor sync). Constants/API stay in `user32.zig` until moved.

pub const split_anchor: u8 = 0;
